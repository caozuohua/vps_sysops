## 变更说明

<!-- 简洁描述本 PR 做了什么，以及为什么这样改 -->

## 关联 Issue

<!-- 关联相关 Issue，例如：Closes #123 -->

## 变更类型

- [ ] Bug 修复（不破坏现有功能的修复）
- [ ] 新功能（添加新的功能）
- [ ] 破坏性变更（修改了现有行为，可能影响使用者）
- [ ] 文档更新（仅文档变更，不涉及脚本逻辑）
- [ ] 重构（不改变功能，只调整代码结构）

## 测试

- [ ] 已在本地运行 `bash tests/smoke.sh`，全部通过
- [ ] 如涉及备份逻辑，已运行 `sudo bash tests/backup_smoke.sh`
- [ ] 已在目标云平台（GCP / Azure）实机验证（如适用）
- [ ] 新增 / 修改的脚本已用 `shellcheck` 检查

## 安全检查

- [ ] 确认本 PR **未包含** `.env`、`*.key`、`*.pem`、Token、数据库文件或 `*.local.conf`
- [ ] 如涉及权限变更，已在 README 和脚本 `--help` 中说明是否需要 root

## 文档更新

- [ ] 已更新 `README.md` 的相关章节（如新增模块或配置项）
- [ ] 已在 `CHANGELOG.md` 的 `[Unreleased]` 节追加变更记录

## 其他备注

<!-- 审查者需要特别关注的点，或者尚未完成的部分 -->
